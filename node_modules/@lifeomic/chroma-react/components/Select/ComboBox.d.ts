import { SelectOptionProps } from './SelectOption';
import { SelectProps } from './Select';
import * as React from 'react';
export interface ComboBoxProps extends Omit<SelectProps, 'onChange' | 'selectedOptionDisplay' | 'value'> {
    onChange?: (values: Array<string>, meta: Array<string>) => void;
    selectedOptionDisplay?: (options: Array<SelectOptionProps>) => string | null | undefined | React.ReactNode;
    value?: Array<string> | undefined;
}
export declare const ComboBox: React.FC<ComboBoxProps>;
