import { InfiniteScrollProps } from '../../hooks/events/useInfiniteScroll';
export interface Props extends InfiniteScrollProps {
    className?: string;
    children: any;
}
export declare const InfiniteScroll: ({ children, className, ...props }: Props) => JSX.Element;
