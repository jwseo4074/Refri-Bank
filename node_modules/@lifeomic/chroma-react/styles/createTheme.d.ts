import { Theme as MuiTheme, ThemeOptions as MuiThemeOptions } from '@material-ui/core/styles/createMuiTheme';
import { Omit } from '../typeUtils';
import { BoxShadows, BoxShadowsOptions } from './createBoxShadows';
import { Palette, PaletteOptions } from './createPalette';
import { Typography, TypographyOptions } from './createTypography';
import { OverridesCreator } from './overrides';
export interface Theme extends Omit<MuiTheme, 'palette'> {
    palette: Palette;
    typography: Typography;
    boxShadows: BoxShadows;
    pxToRem: (size: number) => string;
}
export interface ThemeOptions extends Omit<MuiThemeOptions, 'overrides' | 'palette' | 'typography'> {
    palette?: PaletteOptions;
    typography?: TypographyOptions;
    overrides?: OverridesCreator;
    boxShadows?: BoxShadowsOptions;
    pxToRem?: (size: number) => string;
}
export declare const createTheme: ({ palette, typography, overrides, boxShadows, pxToRem, ...muiOptions }?: ThemeOptions) => Theme;
export declare const useTheme: () => Theme;
