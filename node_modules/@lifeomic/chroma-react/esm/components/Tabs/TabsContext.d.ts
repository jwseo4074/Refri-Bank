import * as React from 'react';
import { TabStateReturn } from 'reakit/ts';
export interface TabsState {
    tabState: TabStateReturn;
    variant: 'default' | 'pill';
    fullWidth?: boolean;
}
export declare const TabsContext: React.Context<TabsState>;
