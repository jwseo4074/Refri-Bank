export declare const helpers: {
    getParentSize: (ref: any) => any;
    getMySize: (ref: any) => any;
};
export declare type InfiniteScrollProps = {
    loading: boolean;
    hasNextPage: boolean;
    loadMore: () => Promise<any>;
    scrollOffset?: number;
    debounceInterval?: number;
    scrollContainer?: 'window' | 'parent';
};
declare function useInfiniteScroll({ loading, hasNextPage, loadMore, scrollOffset, debounceInterval, scrollContainer, }: InfiniteScrollProps): any;
export default useInfiniteScroll;
