import React from 'react';
/**
 * Function that merges the provided refs into one.
 */
export declare const mergeRefs: <T>(...refs: (React.Ref<T> | undefined)[]) => (instance: T | null) => void;
/**
 * Returns a ref callback that merges the provided refs using `mergeRefs`.
 */
export declare const useMergedRefs: <T>(...refs: readonly (React.Ref<T> | undefined)[]) => (instance: T | null) => void;
