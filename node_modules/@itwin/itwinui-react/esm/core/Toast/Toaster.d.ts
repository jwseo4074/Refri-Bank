import React from 'react';
import { ToastProps } from './Toast';
export declare type ToastOptions = Omit<ToastProps, 'category' | 'isVisible' | 'id' | 'content'>;
export default class Toaster {
    private toasts;
    private lastId;
    positive(content: React.ReactNode, settings?: ToastOptions): void;
    informational(content: React.ReactNode, settings?: ToastOptions): void;
    negative(content: React.ReactNode, settings?: ToastOptions): void;
    warning(content: React.ReactNode, settings?: ToastOptions): void;
    private createToast;
    private removeToast;
    private updateView;
    closeAll(): void;
}
