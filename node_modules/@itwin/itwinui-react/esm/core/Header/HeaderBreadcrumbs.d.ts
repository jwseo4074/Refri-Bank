import React from 'react';
import '@itwin/itwinui-css/css/header.css';
export declare type HeaderBreadcrumbsProps = {
    /**
     * Array of elements, chevrons will be put between them.
     * (expects HeaderButton[])
     * @example
     * [
     *  <HeaderButton key={...} ... />
     *  <HeaderButton key={...} ... />
     * ]
     */
    items: React.ReactNode[];
};
/**
 * Header breadcrumbs, adds chevron between each provided items.
 * @example
 * <HeaderBreadcrumbs
 *   items={[
 *     <HeaderButton key={...} ... />
 *     <HeaderButton key={...} ... />
 *   ]}
 * />
 */
export declare const HeaderBreadcrumbs: (props: HeaderBreadcrumbsProps) => JSX.Element;
export default HeaderBreadcrumbs;
